<UserControl x:Class="MeowTaeEditorGui.TaeEventParamField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MeowTaeEditorGui"
             mc:Ignorable="d" 
             d:DesignHeight="24" d:DesignWidth="300">
    <Grid>
        <!--<ToolBar Name="MainToolbar" ItemsSource="{Binding Path='Value'}"/>-->
        <!--<ListBox ItemsSource="{Binding Path='Value'}"/>-->
        <!--<DataGrid ItemsSource="{Binding ElementName=Value}"/>-->
        <ListView ItemsSource="{Binding ElementName=Value}" DisplayMemberPath="">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBox/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
